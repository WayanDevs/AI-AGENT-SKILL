User prefers in-session model switching over creating separate Hermes profiles. They want to stay in their main profile and switch models from within the session/interface, not by creating new profiles per model. Keep profile list minimal — only 'home' (default) and 'coding' profiles should exist. Extra profiles should not be created for model switching purposes.
§
User prefers Indonesian language. Prefers Windows-style desktop dock (full width, black rgba(0,0,0,1) background, always-centered, extend-height, transparency-mode fixed, show-apps-always-in-the-edge).
§
User prefers token-saver lite mode with mid-level explanations.
§
User workspace untuk semua projek development: /home/yan/Dev/ (bukan /home/yan/hermes/). Setiap update catatan di Obsidian vault /home/yan/obsidian/YanVault/ harus di-commit & push ke git.
§
User wants to ensure Tauri WebkitGTK native styling pitfalls and yt-dlp/ffmpeg local integration fixes are saved into the profile's skill files for future use.
§
Do not make code updates, version bumps, or GitHub pushes/commits until user explicitly commands it.