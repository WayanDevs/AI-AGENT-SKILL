User prefers in-session model switching over creating separate Hermes profiles. They want to stay in their main profile and switch models from within the session/interface, not by creating new profiles per model. Keep profile list minimal — only 'home' (default) and 'coding' profiles should exist. Extra profiles should not be created for model switching purposes.
§
Indonesian-speaking user. Prefer komunikasi dalam Bahasa Indonesia. Suka visual customization desktop — dock ala Windows 11 (full width, background hitam solid, ikon di tengah). Menggunakan token-saver mode lite.
§
User prefers Windows-style dock (full width, black background, centered icons) over macOS-style (small centered dock). When customizing dock, default to: extend-height=true, background-color='rgba(0,0,0,1)', transparency-mode=FIXED, always-center-icons=true, show-apps-always-in-the-edge=true.
§
User menggunakan token-saver mode lite (penghematan ~65%). Suka menghemat token tapi masih butuh penjelasan tingkat menengah.
§
User workspace untuk semua projek development: /home/yan/Dev/ (bukan /home/yan/hermes/). Setiap update catatan di Obsidian vault /home/yan/obsidian/YanVault/ harus di-commit & push ke git.